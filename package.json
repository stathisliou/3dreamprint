{
  "name": "conFusion",
  "private": true,
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1",
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
  }
}
